// Dashboard view example
// This file renders dashboard sections and updates the UI.

export function renderDashboard(device) {
  return `<div class="view-summary">Dashboard view using device: ${device.name} (${device.type})</div>`;
}
